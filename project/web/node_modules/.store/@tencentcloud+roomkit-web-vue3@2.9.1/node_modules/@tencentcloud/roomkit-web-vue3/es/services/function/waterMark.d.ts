import { IRoomService } from '../types';

interface IWaterMark {
    toggleWatermark(enabled: boolean): void;
    dispose(): void;
}
export declare class WaterMark implements IWaterMark {
    private service;
    private isWatermarkEnabled;
    constructor(service: IRoomService);
    private bindEvent;
    private handleRoomStart;
    private handleRoomJoin;
    dispose(): void;
    toggleWatermark(enabled: boolean): void;
    private updateWatermarkVisibility;
    private addStyles;
    private addWatermark;
}
export {};
