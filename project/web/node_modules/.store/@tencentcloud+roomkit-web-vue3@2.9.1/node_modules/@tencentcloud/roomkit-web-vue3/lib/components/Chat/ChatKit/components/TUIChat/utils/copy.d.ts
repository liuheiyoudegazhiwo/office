declare class CopyManager {
    private static instance;
    private savedSelection;
    private constructor();
    static getInstance(): CopyManager;
    saveCurrentSelection(): void;
    private restoreSelection;
    copySelection(defaultContent: string): Promise<void>;
    copyTextOrHtml(content: string, type: 'text' | 'html'): Promise<void>;
}
declare const _default: CopyManager;
export default _default;
