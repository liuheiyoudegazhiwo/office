import { IMessageModel } from '../../../../../../../../node_modules/@tencentcloud/chat-uikit-engine';

declare class ConversationDraftManager {
    private static instance;
    private quoteMessageMap;
    private constructor();
    static getInstance(): ConversationDraftManager;
    setStore(conversationID: string, draftContent: string, abstract: string, quoteMessage?: {
        type: 'quote' | 'reply';
        message: IMessageModel;
    }): void;
    getStore(conversationID: string, setEditorContentCallback: (...args: any[]) => void): void;
    generateAbstract(editorContent: Array<{
        type: string;
        payload: {
            text?: string;
            file?: File;
        };
    }>): string;
    private isEditorNotEmpty;
}
declare const _default: ConversationDraftManager;
export default _default;
