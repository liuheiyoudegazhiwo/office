export type MessageProps = {
    type: 'success' | 'error' | 'warning' | 'info';
    message: string;
    duration?: number;
    zIndex?: number;
};
declare const createInstance: ({ type, message, duration }: MessageProps) => void;
export default createInstance;
