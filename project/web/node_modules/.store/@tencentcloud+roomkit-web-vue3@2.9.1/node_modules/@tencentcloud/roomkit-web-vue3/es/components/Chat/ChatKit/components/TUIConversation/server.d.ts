import { TUIConstants } from '../../../../../../../node_modules/@tencentcloud/tui-core';

export default class TUIConversationServer {
    static instance: TUIConversationServer;
    private onCallParamsMap;
    private onCallCallbackMap;
    constants: typeof TUIConstants;
    constructor();
    static getInstance(): TUIConversationServer;
    getOnCallParams(method: string): any;
    getOnCallCallback(method: string): (() => void) | undefined;
    onCall(method: string, params: Record<string, any>, callback: () => void): void;
    onGetExtension(extensionID: string): {
        weight: number;
        icon: any;
        text: string;
        data: {
            name: string;
        };
        listener: {
            onClicked: (item: any) => void;
        };
    }[] | undefined;
    private createConversation;
    private createGroup;
    private routerForward;
    private generateConversation;
    private hideConversationHeader;
}
