declare class VueTouch {
    dom: HTMLElement;
    callback: (event: TouchEvent) => void;
    isMove: boolean;
    isInOnceTouch: boolean;
    isLazyTap: boolean;
    constructor(el: HTMLElement, binding: any);
    touchstart(event: TouchEvent): void;
    touchmove(): void;
    touchend(event: TouchEvent): void;
}
declare const vTap: {
    mounted(el: HTMLElement, binding: any): VueTouch;
};
export default vTap;
