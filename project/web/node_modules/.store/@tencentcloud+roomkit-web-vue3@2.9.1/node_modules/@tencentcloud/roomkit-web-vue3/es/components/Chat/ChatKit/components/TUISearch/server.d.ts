export default class TUISearchServer {
    constructor();
    onCall(method: string, params: {
        [propsName: string]: string;
    }): Promise<any> | undefined;
    onGetExtension(extensionID: string): any[] | undefined;
    searchFriend(userID: string): Promise<any>;
    searchUser(userID: string): Promise<any>;
    searchGroup(groupID: string): Promise<any>;
    searchGroupMember(groupID: string, userID: string): Promise<any>;
}
