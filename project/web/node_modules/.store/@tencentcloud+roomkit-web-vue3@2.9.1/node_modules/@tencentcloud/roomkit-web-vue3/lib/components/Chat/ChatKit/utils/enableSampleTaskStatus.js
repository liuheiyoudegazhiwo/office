"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const TUIChatEngine = require("@tencentcloud/chat-uikit-engine");
function enableSampleTaskStatus(taskKey) {
  const tasks = TUIChatEngine.TUIStore.getData(TUIChatEngine.StoreName.APP, "tasks");
  if (taskKey in tasks && !tasks[taskKey]) {
    tasks[taskKey] = true;
    TUIChatEngine.TUIStore.update(TUIChatEngine.StoreName.APP, "tasks", tasks);
  }
}
exports.enableSampleTaskStatus = enableSampleTaskStatus;
