import { VNode } from '../../../adapter-vue';
import { default as TOAST_TYPE } from './type';

interface IToast {
    message: string;
    type: string;
    offset?: number;
    duration?: number;
    onClose?(): void;
}
interface IToastReturnType {
    close?(): void;
}
declare const Toast: {
    (options: IToast): IToastReturnType;
    close: typeof close;
};
export declare function close(id: string, userOnClose?: (vm: VNode) => void): void;
export { Toast, TOAST_TYPE };
