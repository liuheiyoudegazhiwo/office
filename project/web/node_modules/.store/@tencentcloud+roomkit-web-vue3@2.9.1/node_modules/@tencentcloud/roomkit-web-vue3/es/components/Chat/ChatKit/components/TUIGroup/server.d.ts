import { TUIConstants, ExtensionInfo } from '../../../../../../../node_modules/@tencentcloud/tui-core';

export default class TUIGroupServer {
    static instance: TUIGroupServer;
    private onCallParamsMap;
    private onCallCallbackMap;
    constants: typeof TUIConstants;
    constructor();
    static getInstance(): TUIGroupServer;
    getOnCallParams(method: string): any;
    getOnCallCallback(method: string): (() => void) | undefined;
    onCall(method: string, params: any, callback: () => void): Promise<void>;
    onGetExtension(extensionID: string): ExtensionInfo[];
    private groupManage;
}
