export type NotificationProps = {
    message: string;
    confirm?: () => Promise<void>;
    cancel?: () => Promise<void>;
    confirmButtonText?: string;
    cancelButtonText?: string;
};
declare const Notification: ({ message, confirm, cancel, confirmButtonText, cancelButtonText, }: NotificationProps) => {
    close: () => void;
};
export default Notification;
