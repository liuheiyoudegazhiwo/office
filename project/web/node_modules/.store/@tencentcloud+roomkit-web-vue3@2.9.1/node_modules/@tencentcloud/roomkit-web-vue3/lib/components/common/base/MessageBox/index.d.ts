export type MessageProps = {
    title: string;
    message: string;
    callback?: () => void;
    duration?: number;
    cancelButtonText?: string;
    confirmButtonText?: string;
};
declare const MessageBox: ({ title, message, callback, duration, cancelButtonText, confirmButtonText, }: MessageProps) => void;
export default MessageBox;
